local addonName, ns = ...

ns.Defaults = {
    frame = {
        point = "CENTER",
        x = 0,
        y = 0,
        width = 1100,
        height = 700,
    },
    selectedClass = "DRUID",
    selectedBracket = 19,
    selectedTab = "BIS",
    selectedSlot = "HEAD",
    selectedPage = "GEAR",
    selectedGearSection = "GEAR",
    selectedGuide = "FIRST_AID",
    minimapAngle = 225,
    xpLockdown = false,
    checklist = {},
}
