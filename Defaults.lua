local addonName, ns = ...

ns.Defaults = {
    frame = {
        point = "CENTER",
        x = 0,
        y = 0,
    },
    selectedClass = "DRUID",
    selectedTab = "BIS",
    selectedSlot = "HEAD",
    xpLockdown = false,
    checklist = {},
}
