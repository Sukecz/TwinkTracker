local addonName, ns = ...

ns.Defaults = {
    frame = {
        point = "CENTER",
        x = 0,
        y = 0,
        width = 1100,
        height = 700,
    },
    selectedClass = "DRUID",
    selectedTab = "BIS",
    selectedSlot = "HEAD",
    selectedPage = "GEAR",
    xpLockdown = false,
    checklist = {},
}
